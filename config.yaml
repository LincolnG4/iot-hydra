apiService:
  address: ":8080"
telemetryAgent:
  queueSize: 11
  maxWorkers: 2
  brokers:
    - name: ligmaMyNats
      type: nats
      address: "nats_main:4222"
      auth:
        method: plain
        user: test
        password: test
